# Exclude large DuckDB data files from Vercel git-based uploads (100MB limit).
# These files are generated by CI for v1 GitHub Pages; Vercel v2 uses Supabase.
public/data/pokemon.duckdb export-ignore
