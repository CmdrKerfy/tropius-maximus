# AI agents & merges to `main`

This file is **tracked in git** (unlike `.cursor/`, which may be gitignored locally).

- **Do not** merge **`v2/supabase-migration`** (or any branch) **into `main`**, open a PR whose target is **`main`** for cutover, or run `git merge` into **`main`** unless the **repository owner explicitly requests that merge** in writing for that action.
- **Commits and pushes** to **`v2/supabase-migration`** (or other feature branches) are fine when the owner asks for them.

Canonical project context: **`CLAUDE.md`** (Conventions + AI agents sections).

Optional local Cursor rule (if present): **`.cursor/rules/merge-main-owner-only.mdc`**.
