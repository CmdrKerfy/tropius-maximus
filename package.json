{
  "name": "pkmn-tcg-db",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:manual-id",
    "test:manual-id": "node --test src/lib/manualCardId.test.mjs",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium",
    "check:quick": "npm run build && npm test",
    "check": "npm run check:quick && npm run test:e2e",
    "failed-sets": "python3 -c \"import duckdb; conn = duckdb.connect('public/data/pokemon.duckdb', read_only=True); [print(r) for r in conn.execute('SELECT set_id, reason FROM failed_sets ORDER BY set_id').fetchall()] or print('None'); conn.close()\"",
    "missing-sets": "python3 -c \"import duckdb; conn = duckdb.connect('public/data/pokemon.duckdb', read_only=True); [print(r) for r in conn.execute(\\\"SELECT s.id, s.name, s.total FROM sets s LEFT JOIN tcg_cards c ON c.set_id=s.id GROUP BY s.id,s.name,s.total HAVING COUNT(c.id)=0 ORDER BY s.id\\\").fetchall()] or print('None'); conn.close()\""
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.29.0",
    "@radix-ui/react-dialog": "^1.1.15",
    "@radix-ui/react-dropdown-menu": "^2.1.16",
    "@supabase/supabase-js": "^2.101.1",
    "@tanstack/react-query": "^5.96.2",
    "lucide-react": "^1.8.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.14.0",
    "sonner": "^2.0.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@tailwindcss/vite": "^4.1.18",
    "@vitejs/plugin-react": "^5.1.1",
    "tailwindcss": "^4.1.18",
    "vite": "^7.2.4"
  }
}
