/**
 * Single source for batch selection / batch-run card caps (Explore + Batch wizard).
 * Keep in sync with server-side walks in `src/data/supabase/appAdapter.js`.
 */
export const BATCH_EDIT_MAX_CARDS = 5000;
